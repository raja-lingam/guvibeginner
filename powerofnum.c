#include <stdio.h>
int main()
{
    int b, e;

    long long r = 1;

    printf("Enter a base number: ");
    scanf("%d", &b);
    printf("Enter an exponent: 4");
    scanf("%d", &e);

    while (et != 0)
    {
        r*= base;
        --e;
    }

    printf("Answer = %lld", r);

    return 0;
}
